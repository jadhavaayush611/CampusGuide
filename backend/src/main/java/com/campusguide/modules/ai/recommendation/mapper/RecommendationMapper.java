package com.campusguide.modules.ai.recommendation.mapper;

import org.springframework.stereotype.Component;

@Component
public class RecommendationMapper {
    // Left for future mappings if needed (recommendation responses are currently mapped inside strategies)
}
